/**
 * FAQs entity reducer.
 * @author Manish Garg <manish@helpshift.com>
 * @created June 1, 2017
 */

define ("reducers/faqsReducer", [],
  function () {
    "use strict";

    const INITIAL_STATE = {};

    return function (state = INITIAL_STATE, action) {
      switch (action.type) {
        default:
          return state;
      }
    };
  }
);
